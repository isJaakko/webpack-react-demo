import React, { Component } from 'react';
import { Alert } from 'antd';

export default class Home extends Component {
    render() {
        return (
            <h1>
                Welcome!
            </h1>
        );
    }
}